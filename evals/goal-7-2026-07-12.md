# Goal 7: cold foreign project

## Goal

> Add a CLI operation named health to the existing App service. It must take no request, return exactly the string ok, and include an automated test named health_returns_ok. Leave the project compiling, all tests passing, and conformant. Work only through your provided tools; do not ask for manual edits. In your final answer, give the external command that runs the new operation.

## Run

| Field                | Value                                             |
| -------------------- | ------------------------------------------------- |
| Date                 | 2026-07-12                                        |
| Harness              | `2882423f9925789badcfedd4f65a6f13dc5294c6`        |
| Harness state        | Clean detached worktree                           |
| Provider/model       | Anthropic / `claude-sonnet-4-6`                   |
| Project              | Fresh `live-eval-strict`, unborn HEAD, no remotes |
| Budget               | 32 turns; no resume                               |
| Result               | Strict green                                      |
| Duration             | 63.419 seconds                                    |
| Model turns          | 16                                                |
| Tool calls           | 17                                                |
| Manual project edits | 0                                                 |
| Rolled-back writes   | 1 compile-gated test attempt                      |

The agent snapshotted on turn 3, before the first write on turn 4. It added a CLI-only `health: Empty -> String` operation, implemented `Ok("ok".to_string())`, and inserted `health_returns_ok` through `edit_rust_item`. The failed first test attempt was rolled back by the all-target compile gate. The repaired Tokio test committed on turn 13; `test` and `verify` passed on turn 14. The final answer supplied:

```sh
cargo run --bin app-cli -- health
```

The operation has no declared port flow and `tool: null`; only the four expected owned files changed: `model.json`, both generated projections, and `rust/app/src/service.rs`.

## Recovery

The operator baseline was `2ab34b7a005216f5a69d9957097530e2e2e4a929`. The agent created `e4fa7d8c52561fdfa497a34ee375769d42d997df`; its sequence-2 snapshot and order refs remained pinned after the final answer. The two snapshot trees are identical, proving the agent checkpoint preceded mutation. HEAD remained unborn, no remote appeared, no mutation journal survived, and the successful session transcript was removed normally.

## Acceptance

Every command exited zero in a new process:

| Check                                                      | Result                             |
| ---------------------------------------------------------- | ---------------------------------- |
| Theseus `diff` from baseline                               | Nonempty expected change set       |
| Theseus `check` / `test` / `verify` / `coverage`           | Green; conformant; 1/1 implemented |
| Cargo workspace all-target check                           | Green, no warnings                 |
| Cargo workspace all-target tests                           | Green, no warnings                 |
| Exact `service::tests::health_returns_ok`                  | 1 passed                           |
| Clippy `--workspace --all-targets --locked -- -D warnings` | Green                              |
| Literal `cargo run --bin app-cli -- health`                | stdout exactly `ok\n`              |

The literal CLI stdout SHA-256 is `dc51b8c96c2d745df3bd5590d990230a482fd247123599548e0632fdbf97fc22`.

## Evidence

Raw provider output remains private and outside the repository. Both raw channels were scanned against the exact in-memory credential and common token/header patterns before sanitized copies were read; every scan was negative. At recording time the private evidence directory was `/tmp/theseus-live-eval-strict-20260712-Nw2Yrh/evidence`.

| Artifact               | SHA-256                                                            |
| ---------------------- | ------------------------------------------------------------------ |
| Sanitized trace        | `1d5bd266949103be6e2314eccb96894c29e18684d69d552a881332145290185f` |
| Sanitized final answer | `4bc0b8de76d6a3b89444f9427da1d135aa2a9861718990b706c9802846991fa6` |
| Recorded command list  | `5a2f628d4508be4b5bc76a82fb36910b8ee019ae65fcfa1f5c9f38c2c840f089` |
| Agent binary           | `eb2968e743407d9f239d969f140c573d941ff22c75769f2adf62c23fd93155ed` |
| Theseus CLI binary     | `7e5685876cdecf861a7dc4e720b873dda5ffdaf5623e8ce6d4bab12523913598` |

## Findings fixed before the strict run

Earlier disposable runs found and drove fixes for agent-owned checkpoint release, missing async test support, ambiguous browse errors, silently coerced tool booleans, vague patch schemas, unformatted handler bodies, warning-producing empty CLIs, and boolean-shaped `tool` descriptions. The strict run used the committed fixes and required `tool: null` during acceptance.
