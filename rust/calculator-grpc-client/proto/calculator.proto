// @generated by `theseus generate` — do not edit by hand.
syntax = "proto3";

package theseus.calculator;

message Operands {
  // Left operand.
  double a = 1;
  // Right operand.
  double b = 2;
}

message CalcResult {
  string value = 1;
}

service Calculator {
  // Add the operands.
  rpc Add (Operands) returns (CalcResult);
  // Subtract the operands.
  rpc Subtract (Operands) returns (CalcResult);
  // Multiply the operands.
  rpc Multiply (Operands) returns (CalcResult);
  // Divide the operands.
  rpc Divide (Operands) returns (CalcResult);
}
